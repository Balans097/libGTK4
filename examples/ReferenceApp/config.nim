################################################################
##  config.nim — конфигурация приложения
##
##  Здесь и только здесь собраны величины, которые в реальном
##  проекте меняются чаще всего: идентификатор приложения,
##  размеры главного окна, CSS-тема. Ни один другой модуль не
##  должен содержать "магических" чисел/строк такого рода —
##  всё идёт через этот модуль.
################################################################


# ============================================================================
# Идентификация приложения
# ============================================================================

const
  appId*      = "org.example.GTK4Demo"
  appVersion* = "1.0"


# ============================================================================
# Главное окно
# ============================================================================

const
  windowDefaultWidth*  = 860
  windowDefaultHeight* = 560


# ============================================================================
# CSS — внешний вид приложения
#
# В небольшом проекте достаточно строкового литерала прямо здесь.
# В более крупном — эту константу можно заменить на:
#   const appCSS* = staticRead("theme.css")
# что подключит содержимое файла theme.css на этапе компиляции
# (и позволит редактировать CSS отдельно, не трогая .nim-код),
# либо загружать его через GResource
# (gtk_css_provider_load_from_resource) для по-настоящему
# крупных приложений с несколькими темами.
# ============================================================================

const appCSS* = """
window {
  background-color: #f5f5f5;
}
.statusbar {
  background-color: #e8e8e8;
  border-top: 1px solid #c0c0c0;
  padding: 2px 8px;
  font-size: 1.0em;
  color: #444444;
}
.editor {
  font-family: monospace;
  font-size: 12pt;
  padding: 4px;
}
"""
